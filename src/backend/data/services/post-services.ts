import { PostCreateInput } from '../inputs/PostCreateInput';
import { IRequestContext } from '../IRequestContext';
import { Post } from '../models/Post';

export function updatePostModel(
  model: Post,
  input: PostCreateInput,
  context: IRequestContext) {
  return undefined;
}
